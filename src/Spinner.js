import './Spinner.css'; // Assuming the CSS for the spinner is in a file named Spinner.css

const Spinner = () => {


  return (
            <div class="loader">
                <div class="spinner"></div>
            </div>
  );
};

export default Spinner;
